language: python

python:
  - "3.4"
  - "3.5"
  - "3.6"

install:
  - pip install -U pip setuptools
  - pip install -U cython numpy pandas biopython pyplink pybgen cyvcf2
  - pip list

script: python setup.py test
